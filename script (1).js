const bars=document.querySelectorAll('.bar');function randomHue(){return Math.floor(Math.random()*360)}function updateBars(){for(const bar of bars){const height=Math.floor(Math.random()*38)+6;bar.style.height=`${height}px`;const hue=randomHue();const color=`hsl(${hue},85%,65%)`;bar.style.background=color;bar.style.boxShadow=`0 0 10px ${color}`}requestAnimationFrame(()=>setTimeout(updateBars,120))}if(bars.length)updateBars();
